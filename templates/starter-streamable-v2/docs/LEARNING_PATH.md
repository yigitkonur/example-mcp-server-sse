# Learning Path

1. Run the server and call `echo` via your MCP client.
2. Add a second tool with `z.object(...)` input schema.
3. Add one resource and one prompt.
4. Replace in-memory event storage with persistent storage.
5. Add your auth layer in front of `/mcp`.
